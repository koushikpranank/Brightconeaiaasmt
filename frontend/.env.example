# Leave unset for local dev (Vite proxies /api to http://127.0.0.1:8000, see vite.config.ts).
# In production, point this at the deployed backend, e.g. https://supply-chain-api.onrender.com
VITE_API_BASE=
